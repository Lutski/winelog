.all_wines.transitions-enabled
  - @wines.each do |wine|
    .specific_wine
      .numse
        = image_tag wine.image.url(:medium)
      .wine_info
        %h1= link_to wine.title, wine
        %hr
        %p
          %b Description:
          = wine.description
