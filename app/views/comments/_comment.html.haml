%h2
  This post has
  = pluralize(@wine.comments.count, "Comment")



- @comments.each do |comment|
  %p
    %b= comment.user.email
  %p= comment.content

  - if comment.user == current_user
    = link_to "Delete", [comment.wine, comment], method: :delete, data: { confirm: " Are you sure?" }
    = link_to "Edit", edit_wine_comment_path(comment.wine, comment)
  %hr



= render 'comments/form'
